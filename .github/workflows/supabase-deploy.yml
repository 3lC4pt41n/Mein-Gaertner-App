name: Supabase Deploy

on:
  push:
    branches: [main]
    paths:
      - 'supabase/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: tsllrwaixvhuadrfsskt

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref $SUPABASE_PROJECT_REF

      - name: Push database migrations
        run: supabase db push

      - name: Deploy Edge Functions
        run: |
          supabase functions deploy ai-plant-scan --no-verify-jwt=false
          supabase functions deploy ai-plant-details --no-verify-jwt=false
          supabase functions deploy ai-healthcheck --no-verify-jwt=false
          supabase functions deploy ai-chat --no-verify-jwt=false
          supabase functions deploy ai-gardener-avatar --no-verify-jwt=false
          supabase functions deploy revenucat-webhook --no-verify-jwt
